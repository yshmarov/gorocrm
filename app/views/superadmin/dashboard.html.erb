Superadmin dashboard

<hr>

<%= link_to superadmin_users_path, class: 'btn btn-outline-secondary' do %>
  <i class="fas fa-users"></i>
  <%= t 'header.users' %>
  <div class="badge badge-info">
    <%= User.count %>
  </div>
<% end %>

<hr>

<%= link_to superadmin_tenants_path, class: 'btn btn-outline-secondary' do %>
  <i class="fas fa-building"></i>
  <%= t 'header.tenants' %>
  <div class="badge badge-info">
    <%= Tenant.count %>
  </div>
<% end %>
