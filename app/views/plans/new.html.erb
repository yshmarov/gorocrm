<div class="card shadow">
  <div class="card-header">
    <h4>
      <%= t("helpers.titles.new").titleize %>
      <%= Plan.model_name.human.titleize %>
    </h4>
  </div>
  <div class="card-body">
    <%= render 'form', plan: @plan %>
  </div>
  <div class="card-footer">
    <%= link_to t("helpers.links.back").capitalize, plans_path, class: "btn btn-sm btn-secondary" %>
  </div>
</div>